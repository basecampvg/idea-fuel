{
  "name": "forge-automation",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered business automation platform",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:web": "pnpm --filter @forge/web dev",
    "dev:server": "pnpm --filter @forge/server dev",
    "build": "pnpm -r run build",
    "build:web": "pnpm --filter @forge/web build",
    "vercel-build": "pnpm db:generate && pnpm build:web",
    "build:server": "pnpm --filter @forge/server build",
    "lint": "pnpm -r run lint",
    "type-check": "pnpm -r run type-check",
    "db:generate": "pnpm --filter @forge/server prisma generate",
    "db:push": "pnpm --filter @forge/server prisma db push",
    "db:migrate": "pnpm --filter @forge/server prisma migrate dev",
    "db:studio": "pnpm --filter @forge/server prisma studio"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "dependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "@types/react": "19.1.0",
    "next": "^15.1.3",
    "tailwindcss": "^4.0.0"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "19.1.0",
      "@types/react-dom": "19.1.0"
    }
  }
}
